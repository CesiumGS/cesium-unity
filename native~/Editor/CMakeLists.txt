file(GLOB_RECURSE CESIUMFORUNITYNATIVEEDITOR_SOURCES CONFIGURE_DEPENDS src/*.cpp)
file(GLOB_RECURSE CESIUMFORUNITYNATIVEEDITOR_HEADERS CONFIGURE_DEPENDS src/*.h)

target_sources(
    CesiumForUnityNative
    PRIVATE
        ${CESIUMFORUNITYNATIVEEDITOR_SOURCES}
        ${CESIUMFORUNITYNATIVEEDITOR_HEADERS}
)

target_include_directories(
    CesiumForUnityNative
    PRIVATE
        src
        include
)
